什么是生成器函数，只要函数里有yield关键字就是生成器函数
调用该生成器函数 返回的就是一个生成器对象
生成器对象是在python解释器转换成字节码的时候就产生了

生成器对象也是实现了迭代器协议的 
生成器是不占内存的，因为只有在计算的时候才会占内存。


生成器定以后
例如
def gen():
	a=1
	b=2
	yield
	xxxx
即使 g=gen() #创建一个生成器对象的时候 是可以像函数一样传递参数的

这里是不执行函数的任何东西的  只有调用next的时候，a=1,b=2才会执行，执行到第一个yield就暂停