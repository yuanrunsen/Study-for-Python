有个最大的区别
django和flask是没有实现web服务器（即没有实现socket这些东西的）的，他们只是在写业务逻辑代码（换句话说他们只是app层），是没法直接启来一个服务的（django和flask可以直接起来的那个是借助了一个简单的web服务器框架而本身是不具备启动功能的），需要借助uwsgi这种。
而tornado 是可以直接部署的，他本身就用epoll这种io多路复用就实现了web服务